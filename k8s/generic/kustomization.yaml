# Generic base — plain manifests, no fixed LoadBalancer address.
# Deploy with:  kubectl apply -k k8s/generic/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vcf-download-tool-gui.yaml
